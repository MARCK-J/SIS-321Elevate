spring.application.name=Elevate
server.port=9999

# Configuración de la base de datos PostgreSQL (local)
#spring.datasource.url=jdbc:postgresql://localhost:5432/elevate
#spring.datasource.username=postgres
#spring.datasource.password=lolsito101

# src/main/resources/application.properties

# Esto le dice a Spring Boot que obtenga la URL de la base de datos de una variable de entorno llamada DB_URL
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuraciones adicionales para SSL, que son importantes en entornos de producción como Render.
# Render requiere SSL para la conexión a la base de datos.
spring.datasource.hikari.ssl=true
spring.datasource.hikari.connection-test-query=SELECT 1

#Desactivacion de OEIV / OSIV
spring.jpa.open-in-view=false

# Configuración de Hibernate
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=none
#spring.jpa.properties.hibernate.format_sql=true

# Configuracion para utilizar el gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=agencia.viajes.oasis@gmail.com
spring.mail.password=auveashvdzrifszk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true